pipeline {
    agent any

    stages {
        stage('pull') {
            steps {
                git branch: 'main', url: 'https://github.com/Rohanchougule9027/Automatic-Files-Organizer-Lambda-S3-SNS.git'
            }
        }
        stage('build') {
            steps {
                echo 'build-job'
            }
        }
        stage('test') {
            steps {
                echo 'test-job'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy-job'
            }
        }
    }
}
